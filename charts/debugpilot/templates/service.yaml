apiVersion: v1
kind: Service
metadata:
  name: debugpilot-api
  labels:
    app: debugpilot-api
spec:
  type: {{ .Values.api.serviceType }}
  selector:
    app: debugpilot-api
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
